package org.generation.italy.recipy.model.repositories;

import org.generation.italy.recipy.model.entities.EatingRegime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EatingRegimeRepositoryJPA extends JpaRepository<EatingRegime, Long> {
}
